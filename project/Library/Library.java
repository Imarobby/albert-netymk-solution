Class Library {
	
	/*
	 * argv[1]:	the name of the directory
	 * argv[2]: the requried tags
	 * argv[3]: optional tags
	 */
	public static void main(String[] argv) {
		if (argv.length < 2) {
			System.
	}
}
