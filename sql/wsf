create table user ( id int auto_increment, username varchar(20) unique, password varchar(20), firstname varchar(20), lastname varchar(20),
					dateofbirth date, email varchar(20), phone varchar(20), primary key (id) );
create table administrator ( id int auto_increment primary key, username varchar(20) unique, password varchar(20));
